development: &app_config

  allowed_origins:
    - 'localhost:3030'

  shippify:
    api:
      key: ''
      secret: ''
    dash:
      email: ''
      password: ''
      # Or also could be instead of email: and password:
      # access_token: ''

  ssl_enabled: false

production:
  <<: *app_config

test:
  <<: *app_config
